using System;
using System.Linq;
using System.Collections;
using System.Collections.Generic;

namespace Ejercicio8
{
    class Ejercicio8
    {
        static void Main(string[] args);
         char Letra; 
         Console.WriteLine("Introduce la letra con la cual vamos a operar");
         Letra = Console.ReadLine();
         if (Letra = a : e : i : o :.u);
         {
            Console.WriteLine("{0} es una vocal", Letra);
            Console.WriteLinea("{0} es una letra minuscula", Letra);
            Console.WriteLine("{0} es un simbolo del alfabeto", Letra);
         }
            
         else
            Console.WriteLine("{0} No es una vocal", Letra);
            Console.WriteLinea("{0} No es una letra minuscula", Letra);
    }
}